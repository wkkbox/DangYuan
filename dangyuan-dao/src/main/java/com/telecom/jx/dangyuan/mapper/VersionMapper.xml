<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.telecom.jx.dangyuan.mapper.VersionMapper">

    <select id="selectVersion" resultType="com.telecom.jx.dangyuan.pojo.po.Version">
        SELECT id,versionId,url
        FROM t_version
        ORDER BY id DESC
        limit 1
    </select>

</mapper>